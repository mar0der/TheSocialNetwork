'use strict';

app.controller('navigationController', function($scope, $location, $interval) {

    //uncomment when ready for testing
    //function refreshPendingRequests() {
    //    console.log('req ' +new Date().toISOString());
    //}

    //refreshPendingRequests();

    //var requestsInterval = $interval(function () {
    //    refreshPendingRequests();
    //}, 2000);

    //$scope.$on('$destroy', function () {
    //    $interval.cancel(requestsInterval);
    //});
});